# TODO

## Open follow-up work

- Add first-class protobuf messages for policy-list and domain-login-list
  subscription events so those streams can send incremental updates instead of
  returning an explicit unsupported error event.
